# 어휘 정밀도 신호 검증 — 2026-08-25

L-1(포괄어 남용) 계측기 4종과 v2.5 KatFish 신호 2종을 우리 코퍼스에서 처음 쟀다.
핸드오프 `handoff-20260825-0710-word-precision-rules-shipped-validation-pending`
우선순위 2·3의 결과다.

## 한 줄

**vague_noun_rate 는 살아남았고(홀드아웃 F1 0.604), 신규 2종은 방향이 뒤집혔다.
그리고 그 뒤집힘의 원인은 신호가 아니라 코퍼스다.**

## 코퍼스 — 재현이 아니라 재정의다

`/tmp/dp_corpus` 는 양쪽 맥에서 사라졌다(실측). 세션 로그에서 다시 만들었고,
**원본 필터를 모르므로 재현했다고 주장하지 않는다.**

| | 원본(08-25 07:10) | 이번 |
|---|---|---|
| 사용자 | 1,500건 276,770자 | 732건 157,193자 → 105조각 |
| 에이전트 | 2,814,379자 | 8,263건 2,100,402자 → 1,406조각 |

필터는 양쪽 대칭이다 — 한글 비율 0.30 이상, 길이 20~2,000자. 첫 추출본은 사용자
쪽이 1,136,003자로 원본의 4배였는데, 붙여넣은 크래시 리포트와 장문 스펙 문서가
89%를 차지했다. 그것들은 사람 문체가 아니라 사람이 옮겨 붙인 기계 출력이다.

앞 세션 기준선(사용자 16.4 대 에이전트 54.5)과 **직접 비교하지 않는다.**

## 분할 순서

조각 인덱스만 보고 design/holdout 을 먼저 갈랐다(seed 20260825, `split.json` 고정).
임계는 design 에서만 고르고 홀드아웃에는 **그대로** 적용했다. T4 판별기가 이 순서를
안 지켜 설계셋 1.0 / 홀드아웃 0.6 을 잘못 읽은 전례가 있다.

- design: 사람 52 + AI 52
- holdout: 사람 53 + AI 53

## 결과

| 신호 | 가정 방향 | 임계 | 홀드아웃 P | R | F1 | AUC |
|---|---|---|---|---|---|---|
| `vague_noun_rate` | 높으면 AI | 67.07 | 0.674 | 0.547 | **0.604** | 0.655 |
| `nominal_dominance` | 높으면 AI | 0.749 | 0.500 | 1.000 | 0.667 | **0.275** |
| `spacing_uniformity` | 낮으면 AI | 0.606 | 0.505 | 0.925 | 0.653 | **0.355** |
| `noun_string_max` | 높으면 AI | 3 | 0.500 | 0.981 | 0.662 | **0.237** |
| `light_verb_rate` | 높으면 AI | 0.0 | 0.500 | 1.000 | 0.667 | 0.500 |

**F1 0.66 을 성능으로 읽지 말 것.** 클래스가 균형(53:53)이라 "전부 AI"라고 답하는
분류기의 F1 이 0.667 이다. 아래 셋은 정확히 그 축퇴 상태다 — 정밀도 0.500,
재현율 1.0 이 그 서명이다. **판정은 AUC 로 한다.**

### AUC 0.5 미만 = 방향이 반대다

| 신호 | 뒤집은 AUC | 중앙값 (AI 대 사람) |
|---|---|---|
| `noun_string_max` | 0.763 | 4 대 **5** |
| `nominal_dominance` | 0.725 | 0.844 대 **0.901** |
| `spacing_uniformity` | 0.645 | 0.490 대 **0.459** |

셋 다 **사람 쪽이 더 명사 편중이고 더 균일하다.** KatFishNet 이 보고한 방향과 반대다.

## 왜 뒤집혔나 — 신호가 아니라 코퍼스다

우리 "사람 코퍼스"는 사람이 쓴 **산문이 아니라 프롬프트**다. 지시문은 명사를 나열하고
조사를 생략하며 어절이 고르다. 에이전트 응답은 완성된 산문이라 서술어가 더 많다.

| | 사람(30,382어절) | 에이전트(31,422어절) |
|---|---|---|
| nominal | 0.761 | 0.677 |
| predicate | 0.089 | 0.115 |
| other(경로·코드) | 0.132 | 0.197 |

문장 길이 중앙값은 28 대 29 로 사실상 같다 — **길이가 아니라 화행(speech act)이
교란변수다.** KatFish 는 같은 장르에서 사람 글과 LLM 글을 비교했고, 우리는 사람의
짧은 지시와 AI 의 긴 답변을 비교했다. 같은 실험이 아니다.

**결론: 이 코퍼스로는 두 신호를 검증할 수 없다.** 뒤집힌 AUC 를 "역방향으로 쓰면
된다"고 읽지 말 것 — 그건 저자 판별기가 아니라 지시문 판별기다.

## `vague_noun_rate` 는 왜 다른가

중앙값이 AI 67.2 대 사람 **0.0** 으로 갈렸다. 같은 교란을 안고 있지만 차이의 크기가
다르고 방향이 앞 세션 주장(3.3배)과 일치한다. 다만 홀드아웃 재현율 0.547 —
**절반은 놓친다.** 진단 신호로 쓰고 게이트로 올리지 않는다는 기존 판정은 유지된다.

## 치환 사전 (우선순위 3) — 이 코퍼스로는 못 뽑는다

"사용자는 쓰는데 에이전트는 안 쓰는 구체어"를 뽑으려 했으나 나온 것은 셋뿐이었다.

1. **명령형 화행** — 보고하라, 답해라, 판단해라, 출력해, 검토해
2. **주제어** — 스토리보드, 마크다운, 블라인드 (그 주제로 지시해서 나온 것)
3. **절단 artifact** — 알맞, 그러므, 싶은데 (조사 제거의 부산물)

어휘 선호가 아니라 화행 차이다. 검증 세트와 **같은 교란**이다.

핸드오프가 지목한 "덩어리"도 재현되지 않았다 — 사용자 26회 대 에이전트 **22회**로,
"에이전트 거의 0"이 아니다. 정규화하면 16.9 대 1.06(10만자당)이라 방향은 유효하나
절대빈도로 본 "거의 0"은 과장이다.

## 남은 것 — 무엇을 고쳐야 다시 잴 수 있나

**사람이 쓴 산문 코퍼스가 필요하다.** 프롬프트가 아니라 사람이 독자를 두고 쓴 글이라야
장르를 맞출 수 있다. 후보는 발행 원고·에세이인데, AI 초안을 사람이 고친 것이면
라벨이 오염되므로 **사람이 처음부터 쓴 글**을 골라야 한다.

그 코퍼스가 서기 전까지 신규 2종은 `noun_string_max`·`light_verb_rate` 와 같은
칸에 둔다 — 외부 근거는 있고 우리 판별력은 미검증. baseline 셀도 임계값도 만들지
않는다.

## 재현

스크립트와 분할·결과는 `docs/validation_2026-08-25/` 에 함께 두었다.
코퍼스 원문은 개인 대화 로그라 커밋하지 않는다 — 1번을 다시 돌리면 재생성된다.

```
build_corpus.py     # 세션 로그 -> 발화·응답 (홈맥에서 실행)
chunk_corpus.py     # 대칭 필터 + 1,500자 조각화
split.py            # design/holdout (seed 20260825) -> split.json
eval_signals.py     # 임계 선택(design) -> 홀드아웃 적용 -> eval_results.json
lexicon.py          # 치환 사전 후보 -> lexicon_nouns.json
```

`split.json` 을 그대로 두면 분할이 고정되므로, 지표를 바꿔 다시 재도 같은 홀드아웃에
대고 잰다. **분할을 다시 만들면서 성능을 재지 말 것** — 그게 홀드아웃을 태우는 길이다.
